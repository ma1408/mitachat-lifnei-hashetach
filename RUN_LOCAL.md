# בדיקה מקומית

אפשר לפתוח ישירות את `index.html` בדפדפן.

אם רוצים להריץ שרת מקומי מהתיקייה:

```powershell
node local-server.js
```

ואז לפתוח:

```text
http://127.0.0.1:8080/
```

הטופס הוא MVP מקומי בלבד: הוא מציג הודעת הצלחה בדף ולא שולח נתונים לשרת.
